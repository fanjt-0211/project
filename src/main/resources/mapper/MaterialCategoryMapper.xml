<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fjt.mapper.MaterialCategoryMapper">
    <insert id="insert" parameterType="com.fjt.pojo.entity.MaterialCategory">
        INSERT INTO material_category(name, description, parent_id, sort_order, status)
        VALUES(#{name}, #{description}, #{parentId}, #{sortOrder}, #{status})
    </insert>

    <update id="update" parameterType="com.fjt.pojo.entity.MaterialCategory">
        UPDATE material_category
        <set>
            <if test="name != null and name != ''">name=#{name},</if>
            <if test="description != null">description=#{description},</if>
            <if test="parentId != null">parent_id=#{parentId},</if>
            <if test="sortOrder != null">sort_order=#{sortOrder},</if>
            <if test="status != null">status=#{status},</if>
        </set>
        WHERE id=#{id}
    </update>

    <select id="findById" resultType="com.fjt.pojo.entity.MaterialCategory">
        SELECT * FROM material_category WHERE id=#{id}
    </select>

    <select id="search" resultType="com.fjt.pojo.entity.MaterialCategory">
        SELECT * FROM material_category
        <where>
            <if test="status != null">
                status = #{status}
            </if>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="parentId != null">
                AND parent_id = #{parentId}
            </if>
        </where>
        ORDER BY sort_order, id
    </select>
</mapper>
